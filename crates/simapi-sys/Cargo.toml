[package]
name = "simapi-sys"
description = "Build-time simapi layout checks and field offsets for the checked-out submodule"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
repository.workspace = true
version.workspace = true

[dependencies]
libc = "0.2"

[build-dependencies]
bindgen = "0.71"
cc = "1"
